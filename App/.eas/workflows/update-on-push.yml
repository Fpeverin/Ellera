name: Aggiorna app automaticamente
on:
  push:
    branches: ['main']

jobs:
  update:
    name: Pubblica aggiornamento OTA
    type: update
    params:
      branch: production
      message: ${{ github.event.head_commit.message }}
